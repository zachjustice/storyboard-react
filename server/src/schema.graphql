type Option {
  id: ID!
  description: String!
  next: ID!
}

type Choice {
  id: ID!
  content: String!
  options: [Option]!
}

type Query {
  getChoices(choiceId: ID!, subLevels: Int = 0): [Choice]
  ip(args: String = ""): String
}
